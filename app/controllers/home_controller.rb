class HomeController < ApplicationController
  def index
  end
  def show
    # check_subdomain = Domain.find_by(subdomain: params[:username])
    # if params[:username].present? &&  params[:username].blank? 
    # else

    # end
  end
end
